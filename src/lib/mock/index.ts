/**
 * Mock 数据入口(口子)。
 *
 * Phase 3 将按域补齐并从此处汇出:
 *   models.ts        Model[] / PricingPlan[]
 *   customers.ts     Customer[]
 *   usage.ts         UsageRecord[]
 *   recommendations.ts  Recommendation[](含 evidenceChain)
 *   scripts.ts       TalkScript[]
 *   sessions.ts      CallSession[]
 *   announcements.ts Announcement[]
 *   metrics.ts       Metric[]
 *
 * 约定:全部脱敏假数据;UI 只从这里取数,未来替换为 API 调用即可。
 */
export {};
